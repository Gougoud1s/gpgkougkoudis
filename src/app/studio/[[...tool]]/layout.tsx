export const metadata = {
  title: "Studio — GP. ΓΚΟΥΓΚΟΥΔΗΣ",
  robots: { index: false, follow: false },
};

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
